// ============================================================================
// CLIENT EXPORTS - Better Auth for browser environments
// ============================================================================

export { authClient, signIn, signOut, signUp, useSession, isAuthenticated, getCurrentUser } from '../client';
export type { Session, User } from 'better-auth';
